<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:theme">@android:style/Theme.Black.NoTitleBar</item>
    </style>


    <style name="floating_la">

        <item name="helperTextColor">
            #ffffffff
        </item>

        <item name="errorColor">
            #FF575D
        </item>

        <item name="android:textSize">
            12sp
        </item>

        <item name="secondaryColor">
            #61FFFFFF
        </item>
        <item name="primaryColor">
            #1EF0D7
        </item>

    </style>

    <style name="input_text">

        <item name="android:textSize">
            16sp
        </item>

        <item name="android:textColor">
            #ffffffff
        </item>

    </style>

    <style name="Toolbar">
        <item name="android:layout_height">?attr/actionBarSize</item>
        <item name="android:background">@color/toolBarBack</item>
        <item name="android:layout_width">match_parent</item>
    </style>

</resources>
